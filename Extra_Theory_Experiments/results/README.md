# Results directory

This directory intentionally remains tracked.  Colab checkpoints and compact
CSV/Parquet outputs belong here (or in a copied results directory), and are
never hidden by .gitignore.  Delete the enclosing Extra_Theory_Experiments
folder to remove all disposable experiment additions.

